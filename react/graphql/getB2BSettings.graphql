query GetB2BSettings {
  getB2BSettings @context(provider: "syatt.b2b-organizations-graphql") {
    autoApprove
    defaultPaymentTerms {
      id
      name
    }
    defaultPriceTables
    uiSettings {
      showModal
      clearCart
    }
  }
}
